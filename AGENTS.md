Preserve the protocol and mathematical family version semantics. Submission data must
never become executable code, shell text, paths outside the archive, or workflow YAML.
Production admission is fail closed without a published measured calibration. Keep
development fixtures outside the production epoch registry. Run `python -m unittest
discover -s tests -v` and the offline demo after changes to admission or persistence.
Do not push, change remote settings, or publish without explicit user authorization.
